using lib.net;
using lib.net.Packet;

namespace lib.Routing
{
    internal class SyncPacketRouter
    {
        internal static void Route(SyncPacket syncPacket, NetClient client)
        {
            throw new NotImplementedException();
        }
    }
}